'use client';

import styled from 'styled-components';

const SiderContent = styled.div`
  width: 30%;
`;

export const Sider = () => {
  return <SiderContent>22222</SiderContent>;
};

export default Sider;
